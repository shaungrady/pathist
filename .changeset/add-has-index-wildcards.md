---
"pathist": minor
---

Add `hasIndexWildcards` boolean property to Pathist instances. This property indicates whether the path contains any wildcard index tokens (like `*` or `-1` by default), making it easy to check for wildcards without manually inspecting segments.
